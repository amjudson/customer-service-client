export enum Roles {
  ADMIN = 'admin',
  CLIENT_ADMIN = 'client_admin',
  CUSTOMER = 'customer',
}

export enum Size {
  NUMBER = 'number',
  X_SMALL = 'x-small',
  SMALL = 'small',
  MEDIUM = 'medium',
  LARGE = 'large',
  X_LARGE = 'x-large',
}

export enum InputType {
  TEXT = 'text',
  NUMBER = 'number',
  EMAIL = 'email',
  PASSWORD = 'password',
  CHECKBOX = 'checkbox',
  RADIO = 'radio',
  DATE = 'date',
  TIME = 'time',
  DATETIME_LOCAL = 'datetime-local',
  MONTH = 'month',
  WEEK = 'week',
  URL = 'url',
  SEARCH = 'search',
  TEL = 'tel',
  COLOR = 'color',
}
